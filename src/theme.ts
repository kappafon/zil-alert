import { createMuiTheme } from '@material-ui/core/styles';

export const theme = createMuiTheme({
    palette: {
        type: 'dark',
        background: {
            default: '#1A1A1A'
        },
        primary: {
            main: '#29CCC4'
            // main: '#29CCC4'
        },
        secondary: {
            // main: '#14677A'
            main: '#CC2931'
        }
    }
});
